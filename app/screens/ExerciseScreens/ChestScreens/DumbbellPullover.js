import React from 'react';
import { StyleSheet, Text, Image, View, Button, TouchableOpacity, ScrollView, TextInput } from 'react-native';

function DumbbellPullover(props) {
    return (
        <View>
            <Text>
                DumbbellPullover
            </Text>    
        </View>
    );
}

export default DumbbellPullover;