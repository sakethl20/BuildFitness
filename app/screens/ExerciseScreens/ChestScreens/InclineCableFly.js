import React from 'react';
import { StyleSheet, Text, Image, View, Button, TouchableOpacity, ScrollView, TextInput } from 'react-native';

function InclineCableFly(props) {
    return (
        <View>
            <Text>
                InclineCableFly
            </Text>   
        </View>
    );
}

export default InclineCableFly;