import React from 'react';
import { StyleSheet, Text, Image, View, Button, TouchableOpacity, ScrollView, TextInput } from 'react-native';

function DumbbellInclineBenchPress(props) {
    return (
        <View>
            <Text>
                DumbbellInclineBenchPress
            </Text>   
        </View>
    );
}

export default DumbbellInclineBenchPress;